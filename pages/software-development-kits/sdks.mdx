---
name: SDKs
---

import sdks from '../../src/sdks'
import Sdk from '../../src/components/Sdk'

# Aserto SDKs

<div>
  {sdks.map((s) => 
    <div style={{
      margin: 20,
    }}>
      <Sdk
        title={s.title}
        icon={s.icon}
        github={s.github}
        docs={s.docs}
        sample={s.sample}
        />
    </div>
  )}
</div>
